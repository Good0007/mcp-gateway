# 插件市场页面设计

## 页面布局

```
┌────────────────────────────────────────────────┐
│  🔍 搜索框                    [分类▼] [排序▼]  │
├────────────────────────────────────────────────┤
│  📦 官方推荐  🌟 热门  🆕 最新  📝 全部        │
├────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 🧮       │  │ 🗄️       │  │ 🔍       │    │
│  │Calculator│  │Filesystem│  │ Search   │    │
│  │          │  │          │  │          │    │
│  │ ⭐ 4.8   │  │ ⭐ 4.9   │  │ ⭐ 4.7   │    │
│  │ [已安装] │  │ [安装]   │  │ [安装]   │    │
│  └──────────┘  └──────────┘  └──────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 🧠       │  │ 💬       │  │ ☁️       │    │
│  │ Memory   │  │ Slack    │  │ AWS      │    │
│  │          │  │          │  │          │    │
│  │ ⭐ 4.6   │  │ ⭐ 4.5   │  │ ⭐ 4.4   │    │
│  │ [安装]   │  │ [安装]   │  │ [安装]   │    │
│  └──────────┘  └──────────┘  └──────────┘    │
└────────────────────────────────────────────────┘
```

## 插件卡片组件

```tsx
interface PluginCard {
  id: string;
  name: string;
  icon: string;
  description: string;
  category: string;
  rating: number;
  downloads: number;
  installed: boolean;
  official: boolean;
  tags: string[];
}
```

### 卡片样式

```
┌─────────────────────────┐
│ 🎖️ 官方               ⭐│  
│                          │
│     🧮 Calculator        │
│                          │
│  快速数学计算            │
│  支持基础运算            │
│                          │
│  ⭐ 4.8  📥 1.2k         │
│  #计算 #数学             │
│                          │
│  [ ✓ 已安装 ]  [详情 >] │
└─────────────────────────┘
```

## 插件详情弹窗

```
┌────────────────────────────────────┐
│  Calculator                    [✕] │
├────────────────────────────────────┤
│  🎖️ 官方插件    ⭐ 4.8 (324评价)  │
│                                    │
│  📋 描述                           │
│  提供基础数学计算功能，支持加减   │
│  乘除、平方根等运算。              │
│                                    │
│  📦 安装方式                       │
│  • Stdio (npx)                     │
│  • SSE                             │
│  • Embedded                        │
│                                    │
│  🛠️ 配置示例                       │
│  {                                 │
│    "type": "sse",                  │
│    "url": "http://localhost:8931" │
│  }                                 │
│                                    │
│  📊 依赖                           │
│  无                                 │
│                                    │
│  [  安装  ]           [取消]      │
└────────────────────────────────────┘
```

## 分类列表

```typescript
const categories = [
  { id: 'all', name: '全部', icon: '📝' },
  { id: 'search', name: '搜索', icon: '🔍' },
  { id: 'database', name: '数据库', icon: '🗄️' },
  { id: 'cloud', name: '云服务', icon: '☁️' },
  { id: 'communication', name: '通讯', icon: '💬' },
  { id: 'dev-tools', name: '开发工具', icon: '🛠️' },
  { id: 'ai', name: 'AI服务', icon: '🤖' },
  { id: 'finance', name: '金融', icon: '💰' },
  { id: 'custom', name: '自定义', icon: '⚙️' },
];
```

## 搜索与过滤

### 搜索框
```
[🔍 搜索插件名称、描述、标签...             ]
```

### 过滤器
```
分类: [全部▼]
排序: [热门度▼] [评分▼] [下载量▼] [最新▼]
标签: [官方] [社区] [已安装] [语音友好]
```

## 状态管理

```typescript
interface MarketState {
  plugins: Plugin[];
  loading: boolean;
  filters: {
    search: string;
    category: string;
    sortBy: 'popular' | 'rating' | 'downloads' | 'latest';
    tags: string[];
  };
  selectedPlugin: Plugin | null;
}
```

## API 接口

```typescript
// 获取插件列表
GET /api/plugins?category=all&sort=popular

// 安装插件
POST /api/plugins/:id/install
{
  "type": "stdio",
  "config": { ... }
}

// 卸载插件
DELETE /api/plugins/:id

// 获取插件详情
GET /api/plugins/:id
```

## 交互流程

### 安装流程
1. 点击"安装"按钮
2. 选择安装方式（Stdio/SSE/HTTP）
3. 配置参数（可选）
4. 显示安装进度
5. 安装成功 → 启用服务
6. 失败 → 显示错误信息

### 卸载流程
1. 点击"卸载"按钮
2. 确认对话框
3. 停止服务
4. 删除配置
5. 更新 UI 状态

## 插件元数据

```typescript
interface PluginMetadata {
  id: string;
  name: string;
  version: string;
  author: string;
  description: string;
  icon: string;
  category: string;
  tags: string[];
  rating: number;
  downloads: number;
  official: boolean;
  installMethods: ('stdio' | 'sse' | 'http' | 'embedded')[];
  dependencies?: string[];
  configSchema?: JSONSchema;
  readme?: string;
  homepage?: string;
  repository?: string;
}
```
