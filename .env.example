# ===========================================
# MCP Agent 环境变量配置
# ===========================================
# 
# 使用方法：
#   1. 开发环境：复制此文件为 .env.local (推荐，不会被提交到 Git)
#   2. 生产环境：复制此文件为 .env 或通过 Docker 环境变量配置
#   3. 服务器会自动查找并加载环境变量文件
#
# 模块说明：
#   [Core/Server] - API 服务器和核心功能
#   [CLI] - 命令行工具
#   [All] - 所有模块通用
#
# ===========================================

# [CLI] 配置文件路径（可选，默认为./config/mcp-agent.config.json）
# CLI 工具使用此配置文件进行服务管理
MCP_GATEWAY_CONFIG=./config/mcp-gateway.config.json

# ==================== 日志配置 ====================

# [All] 日志级别 (debug | info | warn | error)
LOG_LEVEL=info

# [All] 日志文件路径（可选，留空则只输出到控制台）
LOG_FILE=

# ==================== 认证配置 (Server) ====================

# [Server] Web UI 登录认证（默认关闭）
# 启用后，访问 Web UI 需要登录
MCP_GATEWAY_AUTH_ENABLE=false

# [Server] 登录用户名（默认：admin）
MCP_GATEWAY_USERNAME=admin

# [Server] 登录密码（默认：admin）
# ⚠️  生产环境请务必修改默认密码！
MCP_GATEWAY_PASSWORD=admin

# ==================== 其他服务配置 ====================

# 以下环境变量可按需配置，用于各个 MCP 服务插件
# SEARCH_API_KEY=your_search_api_key
# DATABASE_URL=your_database_url
# OPENAI_API_KEY=your_openai_api_key

